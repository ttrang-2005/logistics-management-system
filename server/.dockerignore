# Bỏ qua thư viện node_modules (Docker sẽ tự cài lại)
node_modules

# Bỏ qua file build cũ (đối với React/Vite)
dist
build

# Bỏ qua file môi trường cục bộ (để dùng biến môi trường từ Docker Compose)
.env
.env.local
.env.development
.env.test
.env.production

# Bỏ qua lịch sử git
.git
.gitignore

# Bỏ qua log lỗi
npm-debug.log
yarn-debug.log
yarn-error.log

# Bỏ qua file cấu hình của IDE (VS Code,...)
.vscode
.idea

# Bỏ qua file hệ thống
.DS_Store
Thumbs.db

# Bỏ qua chính file Docker (tùy chọn, giữ cho gọn)
Dockerfile
docker-compose.yml