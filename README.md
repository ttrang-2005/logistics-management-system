🚛 CITA Logistics - Hệ thống Quản lý Vận chuyển & Kho hàngCITA Logistics là giải pháp phần mềm toàn diện dành cho các doanh nghiệp vận tải vừa và nhỏ, hỗ trợ quản lý quy trình từ đặt hàng, điều phối vận chuyển đến quản lý kho bãi và báo cáo doanh thu1111.🌟 Tính năng chínhQuản lý Đơn hàng: Tạo, sửa, hủy và theo dõi trạng thái đơn hàng (Đã tạo -> Đang vận chuyển -> Giao thành công)2222.Quản lý Kho hàng: Nhập kho, Xuất kho và Kiểm tra tồn kho thời gian thực3333.Quản lý Tài khoản: Phân quyền Quản trị viên (Admin) và Khách hàng (Customer)4444.Báo cáo Thống kê: Biểu đồ doanh thu trực quan, phân tách doanh thu thực nhận và công nợ COD5.🛠️ Công nghệ sử dụngFrontend: ReactJS (Vite)6666.Backend: Node.js + Express7777.Database: MySQL 8.08.DevOps: Docker & Docker Compose9.🚀 Hướng dẫn Cài đặt & Chạy (Sử dụng Docker)Dự án đã được đóng gói hoàn chỉnh. Bạn không cần cài đặt Node.js hay MySQL trên máy, chỉ cần Docker Desktop.Bước 1: Chuẩn bịĐảm bảo bạn đã cài đặt Docker Desktop và nó đang hoạt động (icon cá voi xanh).Bước 2: Tải dự ánClone repository này về máy tính:Bashgit clone https://github.com/Ten-Cua-Ban/cita-logistics.git
cd cita-logistics
Bước 3: Chạy ứng dụngMở Terminal tại thư mục gốc của dự án và chạy lệnh:Bashdocker-compose up --build
Lần đầu chạy sẽ mất vài phút để tải thư viện và khởi tạo Database.Khi thấy thông báo: Server đang chạy cổng 8081... và ready for connections, bạn có thể truy cập:👉 Trang web: http://localhost:3000👉 API Server: http://localhost:8081🔐 Tài khoản DemoDưới đây là các tài khoản có sẵn trong hệ thống để bạn kiểm thử:Vai tròTên đăng nhậpMật khẩuChức năng truy cậpQuản trị viênadmin123456Toàn quyền (Kho, Đơn hàng, Báo cáo)Khách hàngnguyenvana123456Đặt hàng, Xem lịch sử đơnKhách hàngtranthib123456Đặt hàng, Xem lịch sử đơn📂 Cấu trúc dự áncita-logistics/
├── client/                 # Frontend (React + Vite)
│   ├── src/                # Mã nguồn React
│   ├── nginx.conf          # Cấu hình Web Server
│   └── Dockerfile          # Cấu hình đóng gói Client
├── server/                 # Backend (Node.js + Express)
│   ├── db.js               # Kết nối Database (Connection Pool)
│   ├── index.js            # API Endpoints
│   └── Dockerfile          # Cấu hình đóng gói Server
├── db.sql                  # Script tạo bảng & dữ liệu mẫu
├── docker-compose.yml      # File cấu hình chạy toàn bộ hệ thống
└── README.md               # Hướng dẫn sử dụng
❓ Câu hỏi thường gặp (Troubleshooting)1. Làm sao để xóa sạch dữ liệu và chạy lại từ đầu?Nếu bạn muốn reset database về trạng thái ban đầu (như trong file db.sql), hãy chạy lệnh:Bashdocker-compose down -v
docker-compose up --build
(Lệnh -v sẽ xóa volume dữ liệu cũ).2. Lỗi font chữ tiếng Việt (bị lỗi ký tự lạ)?Dự án đã được cấu hình utf8mb4 chuẩn. Nếu vẫn bị lỗi, hãy đảm bảo bạn đã thực hiện bước 1 (xóa sạch dữ liệu cũ bằng docker-compose down -v) để Docker nạp lại cấu hình bảng mã mới.3. Lỗi "Port is already allocated"?Nếu báo lỗi cổng 3000 hoặc 8081 đã được sử dụng, hãy tắt các ứng dụng khác đang chạy trên cổng này, hoặc sửa file docker-compose.yml (ví dụ: đổi "3000:80" thành "3001:80").👨‍💻 Tác giảSinh viên thực hiện: Lê Thị Thu Trang 10& Phạm Linh Chi11.Trường: Đại học Thăng Long12.Đồ án: Web Quản lý và Vận chuyển Hàng hóa13.
